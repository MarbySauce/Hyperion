Most Egregious Functions in mainFunctions: 
------------------------------------------
	desired_ir_energy_input()
	update_accumulated_image_display()
	update_counter_displays()
	run_melexir()
	scale_and_normalize_pes()
	chart_spectrum_results()
	display_scan_information()
	start_action_scan()
	move_to_ir()
	measure_wavelength()

	wait_for_sevi_scan() and wait_for_melexir() should be fn's executed after scan is done,
		not fn's that check if it's done 